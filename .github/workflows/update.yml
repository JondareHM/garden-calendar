name: Update garden calendar

on:
  push:
    paths:
      - garden.yaml
      - initial_state.yaml
      - generate_calendar.py
      - generate_with_initial_state.py
      - add_year_separators.py
      - requirements.txt
      - .github/workflows/update.yml
  workflow_dispatch:
  schedule:
    - cron: "17 2 * * *"

permissions:
  contents: write

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.x"
          cache: pip

      - name: Install dependencies
        run: python -m pip install -r requirements.txt

      - name: Generate calendar and overview
        run: |
          # Both generated artifacts use the current garden state before 2027.
          python generate_with_initial_state.py
          python add_year_separators.py

      - name: Commit calendar and overview if changed
        run: |
          if git diff --quiet -- calendar/have.ics index.html; then
            echo "No calendar or overview changes"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add calendar/have.ics index.html
          git commit -m "Update garden calendar and overview"
          git push
